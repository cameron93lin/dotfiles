# Config for themes (loaded by plugins)
# Powerlevel10k is loaded in plugins.zsh

# PROMPT fallback if p10k fails or isn't used
PROMPT="%{$fg_bold[cyan]%}%n%{$reset_color%}@%{$fg_bold[cyan]%}%m %{$fg_bold[green]%}%1~ %{$reset_color%}%#"
